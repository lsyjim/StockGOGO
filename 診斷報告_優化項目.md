# StockGOGOV2 — 交易決策系統診斷報告

> 版本：v4.5.x ｜ 診斷日期：2026-05-18  
> 定位：輔助交易決策工具，目標提高進場勝率

---

## 執行摘要

本系統架構層次清晰（三層決策引擎 → 自動交易 → 回測驗證），但存在六個**系統性缺陷**，直接壓低理論勝率。其中最關鍵的問題是：**回測引擎與三層決策引擎完全脫鉤**——你測試的是 MA 均線交叉，但實際下單用的是 ThreeLayerEngine，兩者無任何連結。在這種情況下，你對系統真實勝率一無所知。

優先修復項目依影響程度排序如下：

| 優先級 | 問題 | 影響 |
|--------|------|------|
| 🔴 P0 | 回測未連接真實信號 | 無法驗證勝率 |
| 🔴 P0 | 缺乏大盤濾網 | 空頭市場仍觸發買訊 |
| 🟠 P1 | Layer 3 C 級門檻過低 | 大量雜訊信號 |
| 🟠 P1 | 停損邏輯靜態化 | 未用 ATR 動態調整 |
| 🟡 P2 | 自動交易場景 A 否決邏輯錯誤 | 錯殺最強信號 |
| 🟡 P2 | 倉位管理固定化 | 無法反映信號強度 |

---

## 一、決策引擎層 (decision_engine.py)

### 1-A. Layer 1 方向分：MA 計數忽略斜率

**問題**：僅用「多頭層數（0–4）」線性映射分數，未考量均線的**斜率方向**。均線排列剛翻多（斜率尚未向上）與排列已久（斜率穩健向上）應得到不同分數。

```python
# 現況：只計算 bull_count
base_score = 15 + bull_count * 16.25  # 0→4 對應 15→80

# 問題：MA20 > MA60 可能只差 0.01%（剛剛翻多），
# 也可能差 10%（強勢多頭），兩者得分相同
```

**建議修正**：在 bull_count 基礎上加入**MA20 5日斜率**輔助判斷，斜率正且加速 → +5，斜率轉負 → -5。

---

### 1-B. Layer 1 方向分：缺乏大盤連動濾網（最高優先）

**問題**：個股方向分 ≥ 40 就可通過 Layer 1，即使加權指數（^TWII）處於空頭下跌趨勢。歷史數據顯示，大盤空頭期間個股 90% 以上的機率被拖累。

**目前狀態**：`market_trend_manager.py` 與 `MarketRegimeAnalyzer` 存在，但**三層引擎未引用**，形同虛設。

**建議修正**：在 `ThreeLayerEngine.analyze()` 入口處加入大盤狀態判斷：
- 大盤 Bear（MA20 < MA60 < MA120）→ 個股方向分上限調為 55（C 級觀察，不主動買進）
- 大盤 Range（ADX < 20）→ 不發 A 級信號

---

### 1-C. Layer 2 位置分：RR 計算依賴不可靠預設值

**問題**：當 `support_resistance` 模組未返回壓力/支撐位時，系統默認：
```python
take_profit = current * 1.1   # 預設 +10%
stop_loss   = current * 0.95  # 預設 -5%
```
這個 1.1 / 2.0 的 RR = 2.0 是假數字，會讓劣質股票通過位置層。

**建議修正**：當 support_resistance 無有效數據時，fallback 應標記為「RR 計算不可靠」，並對位置分扣 10 分懲罰，而非使用假設值。

---

### 1-D. Layer 2 位置分：深度超跌接刀風險

**問題**：`bias < -10%` 時仍給 40–75 分（位置層通過），可能在下跌趨勢中觸發進場。

**建議修正**：深度超跌時，必須要求 Layer 1 方向分 ≥ 60（偏多以上），否則位置分強制設為 35（否決）。禁止「趨勢空頭 + 深度超跌」組合通過 Layer 2。

---

### 1-E. Layer 3 時機分：C 級門檻過低

**問題**：C 級觸發條件只需 `is_bullish_env == True`，幾乎所有多頭個股都能達到，造成大量「觀察」清單噪音，稀釋 A/B 級信號的注意力。

```python
# 現況
if wave.get('is_bullish_env'):
    grade = 'C'  # 只要多頭環境就是C級，太寬鬆
```

**建議修正**：C 級應要求至少滿足以下其中一項：
- RSI 在 40–65 的「健康回檔區」
- 布林通道帶寬壓縮（VCP 前兆）
- 乖離回歸至 MA20 ±3% 以內

---

### 1-F. 賣訊邏輯：移動停利未實裝

**問題**：`config.py` 中詳細定義了移動停利參數（`TRAILING_STOP_ENABLED`、`TRAILING_STOP_ACTIVATION_PCT = 10%`、`TRAILING_STOP_DISTANCE_PCT = 5%`），但在 `decision_engine.py` 的 `check_sell_signal()` 中**完全未使用**。持倉管理缺乏動態停利機制，已獲利部位在回調時被動等到停損才出場。

**建議修正**：在 `check_sell_signal()` 中新增 TRAILING_STOP 類型賣訊，讀取 `trailing_stop_data.json` 記錄的最高點位，觸發條件為「從最高點回落 ≥ 5%」。

---

## 二、回測系統（backtesting.py）—— 最關鍵問題

### 2-A. 回測與真實信號脫鉤（P0 嚴重缺陷）

**問題**：`BacktestEngine` 只測試 4 個通用策略（MA 均線交叉、RSI、布林通道、均值回歸），**完全沒有對 ThreeLayerEngine 的實際 A/B/C 級信號進行歷史回測**。

這代表：
- 你不知道「A 級主攻」信號的歷史勝率是多少
- 你不知道「W底確立」的真實期望值
- 你無法比較「Layer 2 位置分 ≥ 60」和「≥ 50」哪個閾值更好

**建議修正**：新增 `SignalBacktester` 類別，對指定股票清單執行 ThreeLayerEngine，記錄每次 A/B/C 級信號的後續 5/10/20 日報酬，統計：
- 分級別勝率
- 平均持倉報酬
- 最大逆勢回撤（MAE）

---

### 2-B. 無市場環境分類驗證

**問題**：回測未區分牛市/熊市/盤整環境，無法確認策略是否僅在特定市場條件下有效（即「模型過擬合牛市」）。

**建議修正**：為每段回測期間標記市場環境（Bull/Bear/Range），分別輸出三種環境下的績效，確保策略在各環境均有正期望值或已設計出手保護。

---

### 2-C. 無 Walk-Forward 驗證

**問題**：現有回測為單次全樣本回測（in-sample），存在過度擬合風險。

**建議修正**：實作滾動 Walk-Forward 測試：
- 訓練窗口：12 個月
- 測試窗口：3 個月
- 滾動步進：1 個月

---

## 三、自動交易邏輯 (auto_trader.py)

### 3-A. 場景 A 否決邏輯錯誤（語義錯誤）

**問題**：
```python
# auto_trader.py 買進否決條件
if scenario == 'A':
    vetoes.append(f"場景A過熱（{dm.get('scenario_name', '')}）")
```

場景 A 的定義是「**順勢多頭（Strong Bull）**」，是最強的買進信號。否決場景 A 等於專門過濾掉最好的進場機會。應否決的是場景 D（高檔反轉）。

**建議修正**：
```python
# 應改為
if scenario == 'D':
    vetoes.append(f"場景D高檔反轉，規避風險")
```

---

### 3-B. 停損未使用 ATR 動態計算

**問題**：`auto_trader_settings.json` 硬設 `stop_loss_pct: 8.0%`，對所有股票一視同仁。

- 低波動股（ATR 約 1%）：8% 停損太寬，等於放棄風控
- 高波動股（ATR 約 5%）：8% 停損太窄，容易在正常波動中被洗出

`config.py` 已定義 `ATR_K_STOP = 2.0` 與 `ATRStopLossCalculator`，但 `auto_trader.py` 未使用。

**建議修正**：停損改為 `ATR × 2.0`，並設定上下限（最低 3%，最高 12%）。

---

### 3-C. 倉位固定化，未反映信號強度

**問題**：無論 A 級主攻還是 B 級追蹤，均使用固定 20% 倉位。

**建議修正**（Kelly 分數簡化版）：
| 信號等級 | 倉位 |
|---------|------|
| A 級 + High Confidence | 25–30% |
| A 級 + Medium Confidence | 20% |
| B 級 | 12–15% |
| C 級 | 不主動建倉 |

---

### 3-D. 未設定同一方向最大持倉限制

**問題**：系統可能同時持有多檔高度相關的股票（如同一產業鏈），當板塊系統性下跌時，損失集中爆發。

**建議修正**：新增板塊集中度檢查，同一板塊持倉不超過總資金 40%。

---

## 四、缺失的關鍵模組

### 4-A. 信號品質追蹤系統（最重要的長期基建）

**問題**：目前 `trade_log.json` 只記錄 4 筆交易，且只有進出場資訊，**沒有記錄進場時的信號原因與當時的三層分數**。無法做事後統計分析。

**建議**：擴展 trade_log 格式，每筆交易記錄：
```json
{
  "signal_grade": "A",
  "direction_score": 72,
  "position_score": 65,
  "timing_triggers": ["三盤突破（帶量確認）"],
  "market_regime": "Bull",
  "entry_date": "...",
  "exit_date": "...",
  "exit_reason": "trailing_stop",
  "pnl_pct": 8.3
}
```

---

### 4-B. 板塊輪動未連接決策引擎

**問題**：`trend_scanner.py` 的 `SectorMomentumScanner` 可以識別強勢板塊，但三層決策引擎未使用這個資訊。選股時未優先選擇強勢板塊中的強勢個股（RS 效應）。

**建議**：在 Layer 1 方向分中加入板塊強度因子：
- 所在板塊 5 日動能 > 0 → +5 分
- 所在板塊為當週前 3 名 → +10 分

---

### 4-C. 財報季濾網缺失

**問題**：台股財報公告期（3月、5月、8月、11月）前後個股波動加劇，系統未避開高風險窗口。

**建議**：在財報發布前 5 個交易日，A 級信號降為 B 級，B 級信號暫停買進。

---

## 五、配置參數優化建議 (config.py)

| 參數 | 現值 | 建議值 | 原因 |
|------|------|--------|------|
| `GOLDEN_BUY_RSI_MAX` | 60 | 55 | 60 過寬，容易在中段買進 |
| `ATR_K_STOP` | 2.0 | 2.0–2.5 | 視股票波動性動態選擇 |
| `SLIPPAGE_BASE` | 0.1% | 0.15% | 中小型台股滑價普遍更高 |
| `BIAS_OVERSOLD_THRESHOLD` | -10% | -12% | 現值太高，常在下跌中觸發 |
| `THREE_BAR_LOOKBACK` | 2 | 2 | 維持，合理 |
| `SIGNAL_COOLDOWN_HOURS` | 24 | 48 | 避免隔日反轉再買入 |
| `TRAILING_STOP_ACTIVATION_PCT` | 10% | 8% | 台股波段通常較短 |
| `MIN_AVG_VOLUME_20` | 500張 | 1000張 | 提高流動性門檻，降低衝擊成本 |

---

## 六、優化實施路線圖

### 第一階段：即戰力優化（1–2 週）

1. **修正場景 A 否決邏輯**（auto_trader.py 第 1155 行）— 30 分鐘
2. **接通大盤濾網**（在 ThreeLayerEngine.analyze() 中引用 MarketRegimeAnalyzer）— 2 小時
3. **修正 Layer 3 C 級門檻**（加入 RSI/BB 次要條件）— 1 小時
4. **啟用 ATR 動態停損**（替換固定 8% 停損）— 2 小時

### 第二階段：基礎設施補強（2–4 週）

5. **擴展 trade_log 格式**（記錄信號三層分數）— 1 天
6. **實作移動停利**（讀取 trailing_stop_data.json）— 1 天
7. **修正 RR 計算的 fallback 邏輯** — 半天

### 第三階段：信號驗證系統（1–2 個月）

8. **新增 SignalBacktester**（對三層信號做歷史回測）— 3 天
9. **Walk-Forward 驗證框架** — 3 天
10. **板塊強度連接決策引擎** — 2 天

---

## 附錄：現有真實交易記錄分析

目前 trade_log.json 僅有 4 筆記錄（1月下旬 ~ 2月初）：

| 時間 | 動作 | 股票 | 原因 | 結果 |
|------|------|------|------|------|
| 2026-01-27 | 買入 4939 | 9000 張 @42.57 | W底形態確立 | ❌ 停損 -8.6%，虧損 3.8萬 |
| 2026-01-29 | 買入 2455 | 2000 張 @170.19 | W底形態確立 | 未知（無出場記錄） |
| 2026-02-10 | 買入 3711 | 1000 張 @330.27 | W底確立 | 未知（無出場記錄） |

**觀察**：
- 3 次買進均觸發「W底確立」，顯示系統對底部形態的依賴較高
- 4939 的 W底訊號後來以 -8.6% 停損，可能是在下跌趨勢中接刀（呼應問題 1-D）
- 缺乏大盤同步確認，建議查看 2026-01-27 當天 ^TWII 的狀態

---

*本報告由高盛產業分析師視角產出，建議按優先級逐步實施，每個修正後透過 SignalBacktester 驗證效果。*
